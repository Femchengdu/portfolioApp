import React from "react";
import "./mainNavContainer.css";
const MainNavContainer = ({ children }) => {
  return <nav className="mainNavContainer">{children}</nav>;
};

export default MainNavContainer;
