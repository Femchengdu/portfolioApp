language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t rdevcd2020/portfolioApp -f Dockerfile.dev ./target-portfolio

script:
  - docker run -e CI=true rdevcd2020/portfolioApp yarn test --coverage
